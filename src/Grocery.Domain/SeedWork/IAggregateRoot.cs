namespace Grocery.Domain.SeedWork
{
    /// <summary>
    /// An <see cref="IAggregateRoot"/> constraint that identifies a type which can be used
    /// as the gateway into the aggregate.
    /// </summary>
    public interface IAggregateRoot { }
}
